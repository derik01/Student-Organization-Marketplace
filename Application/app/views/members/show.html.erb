<p id="notice"><%= notice %></p>

<h1><span style = "color:white">Howdy</span></h1>
<% if logged_in_member? %>
    <% if User.find_by_id(current_member.users_id) == nil %>

        <h1><span style = "color:white">You are Logged In, <%= current_member.first %></span></h1>        
        <h2><span style = "color:white">You are currently not in any Organizations.</span></h2>  

    <% else %>
        <h1><span style = "color:white">You are Logged In, <%= current_member.first %></span></h1>
        <h2><span style = "color:white">Organizations:</span></h2>
        <span style = "color:white"><%= User.find_by_id(current_member.users_id).first %></span>
    
    <%end%>
<%end%>

<h3><span style = "color:white">Referral Code: <%= current_member.referral_code %></span></h3>

<h3><span style = "color:white">Number of products you have referred: <%= current_member.num_referred %></span></h3>

<%= link_to 'Edit Profile', "/edit_mem" %> |
<%= button_to 'Logout', logout_path, :method => :delete %>
